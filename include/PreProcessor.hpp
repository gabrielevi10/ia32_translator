#ifndef PREPROCESSOR_HPP
#define PREPROCESSOR_HPP

#include <iostream>
#include <algorithm>
#include <vector>
#include <string>
#include <map>
#include <fstream>

#include "helpers.hpp"

using namespace std;

class PreProcessor {
    public:
        PreProcessor();
        ~PreProcessor();

        void passage_zero(string);

    private:


};

#endif